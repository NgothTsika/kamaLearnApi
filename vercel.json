{
  "buildCommand": "yarn generate && yarn build",
  "outputDirectory": "dist",
  "functions": {
    "dist/api/index.js": {
      "maxDuration": 60
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/api/index.js"
    }
  ]
}
